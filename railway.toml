[build]
builder = "RAILPACK"

[deploy]
runtime = "V2"
numReplicas = 1
sleepApplication = false
useLegacyStacker = false
startCommand = "python3 main.py --http"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
