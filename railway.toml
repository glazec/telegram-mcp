[build]
builder = "RAILPACK"

[deploy]
runtime = "V2"
numReplicas = 1
sleepApplication = false
useLegacyStacker = false
startCommand = "uv run main.py --http --host $MCP_HTTP_HOST --port $MCP_HTTP_PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
